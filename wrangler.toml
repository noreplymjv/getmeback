# Cloudflare Pages (free) — Git connected to GitHub
#
# Auto-detected settings MUST be:
#   Build command:  npm run build  →  bash scripts/cf-pages-build.sh
#   Output dir:     build/web
#
# If the dashboard still says output "web", change it to build/web and retry.

name = "getmeback"
compatibility_date = "2026-08-20"
pages_build_output_dir = "build/web"
